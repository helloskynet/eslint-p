const m = asyncid => {
  return await Promise.resolve(id);
};
console.log(m);

// 规则 arrow-parens 和 space-before-function-paren

// Ctrl + S 或 Quick Fix --> fix all autofixable problems
// 格式化之后

// const m = asyncid => {
//   return await Promise.resolve(id);
// };
// console.log(m);
